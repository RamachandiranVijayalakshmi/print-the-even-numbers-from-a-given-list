## print-the-even-numbers-from-a-given-list
## Sample code to check the Given list
```sh
enum = []
    for n in l:
        if n % 2 == 0:
            enum.append(n)
```
## Expected Output
```sh
[2, 4, 6, 8] 
```
